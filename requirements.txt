# Core Frameworks
Django>=4.2,<5.0
djangorestframework>=3.14.0 # For the API endpoints
Pillow>=10.0.0 # Essential for Django's ImageField

# Environment Variables Management
python-decouple>=3.8.0 # Used in settings.py for SECRET_KEY, GEMINI_API_KEY, etc.

# Gemini AI Integration
google-genai>=0.11.0 # The official Google GenAI SDK

# Authentication (Used by Django REST Framework)
djangorestframework-simplejwt>=5.3.0 # Highly recommended for production APIs

# PRODUCTION SERVER
gunicorn>=21.0.0 # **MISSING DEPENDENCY: Required for the 'gunicorn' command to work**